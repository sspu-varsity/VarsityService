<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sspu.varsityservice.mapper.UserMapper">
    <!--    public Account getAcct(Long id); -->
    <select id="userLogin" resultType="com.sspu.varsityservice.pojo.User">
        select * from v_user where user_code=#{userCode} and user_password=#{userPassWord}
    </select>
</mapper>